{
  "name": "the-void-database",
  "version": "0.0.0",
  "description": "The Void's database",
  "main": "./src/starter.ts",
  "scripts": {
    "dev": "nodemon ./src/starter.ts",
    "start": "node ./dist/index.js",
    "build:webpack": "webpack --mode developmen",
    "build": "tsc && tsc --declaration",
    "publish": "npm pack && cd dist/database/src && npm pack --dry-run && npm publish"
  },
  "keywords": [
    "db"
  ],
  "author": "thevoidcommunity",
  "license": "MIT",
  "dependencies": {
    "mongoose": "^8.3.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "nodemon": "^3.1.7",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
